
document.addEventListener('DOMContentLoaded', function() {
  // Obtenha referências aos elementos do formulário
  const formAdicionarTreino = document.getElementById('formAdicionarTreino');
  const exercicioInput = document.getElementById('exercicio');
  const qtdSeriesInput = document.getElementById('qtdSeries');
  const qtdRepeticoesInput = document.getElementById('qtdRepeticoes');
  const cargaInput = document.getElementById('carga');

  // Obtenha referências aos elementos da seção "Consultar Treinos"
  const btnConsultarTreinos = document.getElementById('btnConsultarTreinos');
  const treinosList = document.getElementById('treinosList');

  // Adicione um ouvinte de evento ao formulário "Adicionar Treino"
  formAdicionarTreino.addEventListener('submit', function(event) {
    event.preventDefault(); // Impede o envio do formulário

    // Obtenha os valores dos campos do formulário
    const exercicio = exercicioInput.value;
    const qtdSeries = qtdSeriesInput.value;
    const qtdRepeticoes = qtdRepeticoesInput.value;
    const carga = cargaInput.value;
});
});
